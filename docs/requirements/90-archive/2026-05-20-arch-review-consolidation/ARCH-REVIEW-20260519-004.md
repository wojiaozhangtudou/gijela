# ARCH-REVIEW-20260519-004：对 ARCH-REVIEW-20260519-002 的开工准入评审（最终）

### 一、可落地性结论
- 结论：**可落地（可进入 Phase 1 开发）**。
- 依据：
  - 业务边界已清晰：`gijela-core-chat` 与 `chat-flow` 无业务关系，且方案已按“chat-flow 独立域”收敛。
  - Phase 1 的 P0 前置物已在文档内固化：API 合同、DDL/索引/回滚、错误码、超时与降级口径、测试门槛。
  - 范围已明确：本期不包含权限与审计，避免开发期范围漂移。

### 二、关键问题与调整建议
1. 问题：实现默认值需文档显式固化。
   - 位置：`ARCH-REVIEW-20260519-002` 第七章“默认实现决策（已定）”。
   - 风险：影响实现细节（如历史深度、版本跟随策略），但不阻塞 Phase 1 主链路启动。
   - 建议：按既定默认值执行并在任务单中锁定：
     - 历史深度 = 10
     - 版本策略 = 跟随 `published_version`
     - 会话删除 = 软删
   - 优先级：P1

2. 问题：接口分页/排序细节尚未显式写入合同。
   - 位置：`ARCH-REVIEW-20260519-002` 第 9.1 节。
   - 风险：前后端联调时可能产生边界行为不一致。
   - 建议：补充默认排序规则（按 `updated_at desc` / `created_at asc`）与分页上限（如 `pageSize<=100`）。
   - 优先级：P1

### 三、最小落地路径（MVP）
- 第一步：按 002 文档第九章落 Phase 1 骨架（表结构、接口、Service）。
- 第二步：打通“创建会话→发送消息→写入历史→查询历史”闭环。
- 第三步：执行回归与基线测试，达标后进入 Phase 2。

### 四、已定默认决策
1. 历史透传默认深度固定为 10。
2. 会话历史本期使用页码分页（暂不引入游标分页）。
3. 多输出字段（`sources`、`metadata`）本期仅透传，不展示。

### 五、已落盘位置
- docs/requirements/ARCH-REVIEW-20260519-004.md
- docs/requirements/index.md
