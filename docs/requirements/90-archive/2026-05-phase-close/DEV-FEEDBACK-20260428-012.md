# DEV-FEEDBACK-20260428-012 流式 ToolContext 显式优先回归覆盖缺口

## 问题分类
- 实现风险

## 问题位置
- 需求/架构位置：`REQ-20260427-002` 第三章 P0-2（统一调用链路）与 P0-4（稳定性保障）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/main/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClient.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/test/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClientStreamFunctionCallingTest.java`

## 影响范围
- 功能：`stream()` 的工具执行上下文若未保证显式 `toolContext` 优先，可能误用 metadata 回退上下文。
- 稳定性：chat/stream 语义不一致会导致多租户与审计上下文行为漂移。

## 严重级别
- Minor

## 现象
- 已覆盖 chat 场景显式 `toolContext` 优先，但 stream 场景缺少同类断言测试。

## 修正建议
- 增加流式回归用例，验证 `stream()` 中工具执行优先使用显式 `toolContext`。
- 持续保持 chat/stream 对称测试矩阵。

## 临时绕行方案
- 无需绕行，已直接补齐回归测试。

## 处理结果
- 已新增测试：`shouldPreferExplicitToolContextDuringStreamingToolExecution`。
- 验证结果：
  - 执行 `mvn -pl gijela-core-llm/gijela-core-llm-sdk-openai-compatible -am test -DskipITs`
  - 结果：`Tests run: 31, Failures: 0, Errors: 0`，`BUILD SUCCESS`。
- 结论：该反馈项已闭环。
