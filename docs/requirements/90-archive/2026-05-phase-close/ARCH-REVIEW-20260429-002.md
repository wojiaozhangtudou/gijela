# ARCH-REVIEW-20260429-002 chat 模块开发完成度验收

### 一、可落地性结论
- 结论：可落地，验收通过。
- 依据：
  - 主链路已闭环：同步对话、流式对话、会话历史、失败回合标记、附件上传、知识检索、审计记录均已接入。
  - 基础设施链路已收口：MySQL 表结构迁移已接入 Flyway，附件对象存储已改为真实 HTTP 对接方式，知识检索已改为 Qdrant HTTP 查询方式。
  - 可验证结果已具备：`gijela-core-chat` 全量测试 24/24 通过；`gijela-bloom-chat` 的 `pnpm typecheck`、`pnpm build` 均通过。

### 二、关键问题与调整建议
1. 问题：当前无 P0 阻断项。
   - 位置：无。
   - 风险：无阻断风险。
   - 建议：按当前冻结基线进入联调/交付，不再扩散新功能。
   - 优先级：P0

2. 问题：对象存储采用 HTTP 接口适配方式，而非专用 SDK。
   - 位置：`gijela-core-chat` 附件存储接入层。
   - 风险：后续若 RustFS 接口规范变化，需要同步调整上传请求构造。
   - 建议：在部署文档中明确“当前按 S3/HTTP 接口适配”，后续如官方 SDK 稳定可再评估替换。
   - 优先级：P2

3. 问题：前端仍存在构建体积告警。
   - 位置：`gijela-bloom-chat` 构建输出。
   - 风险：当前不阻断交付，但后续页面扩展时可能影响首屏性能。
   - 建议：后续如继续扩展页面，再评估 `manualChunks` 或动态拆包；本轮不作为阻断项。
   - 优先级：P2

### 三、最小落地路径（MVP）
- 第一步：按当前配置完成测试环境联调（MySQL / Redis / Qdrant / 对象存储服务）。
- 第二步：执行 Flyway 迁移并完成一次端到端上传/检索/会话回放冒烟。
- 第三步：冻结版本，进入验收交付或并行环境验证。

### 四、待拍板决策
1. 对象存储后续是否继续保持 HTTP 适配，还是切换到专用 SDK。
2. 前端打包体积优化是否纳入下一轮迭代。

### 五、已落盘位置
- docs/requirements/ARCH-REVIEW-20260429-002.md
- docs/requirements/index.md
