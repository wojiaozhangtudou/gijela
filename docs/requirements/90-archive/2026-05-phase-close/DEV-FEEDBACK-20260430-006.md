# DEV-FEEDBACK-20260430-006

## 问题分类
- 实现风险

## 问题位置
- 知识库检索触发策略（ChatOrchestratorServiceImpl.searchKnowledgeBase）
- 涉及文件：
  - gijela-core/gijela-core-chat/src/main/java/com/gijela/morpheus/chat/service/impl/ChatOrchestratorServiceImpl.java

## 影响范围
- 功能：用户输入“你好”等寒暄语也会触发向量检索，出现无意义引用。
- 性能：增加不必要的 embedding 与向量检索开销。

## 严重级别
- Major

## 修正建议
1. 增加“是否触发知识库检索”判断。
2. 对常见问候/寒暄词（中英文）直接跳过检索。
3. 对低信息量输入（去标点后长度过短）跳过检索。

## 临时绕行方案
- 无需绕行，已直接修复。

## 本次落地结果
- 新增 `shouldSearchKnowledge()` 与 `normalizeQuery()`。
- 对 `你好/您好/hi/hello/thanks` 等输入不再检索知识库。
- 补充测试验证问候语场景不触发 `knowledgeIngestionService.search()`。
