# ARCH-REVIEW-20260505-010：全链路同步执行决策评审（chat 模块）

- 日期：2026-05-05
- 状态：已评审
- 关联：
  - [ARCH-REVIEW-20260505-007.md](./ARCH-REVIEW-20260505-007.md)
  - [ARCH-REVIEW-20260505-009.md](./ARCH-REVIEW-20260505-009.md)

### 一、可落地性结论
- 结论：**有条件可落地**。
- 依据：
  1. 已明确“全部改为同步”，流程边界清晰；
  2. 手动确认策略与同步导入一致，不存在语义冲突；
   3. 首版已确认“同步先不设置超时”，需重点补齐输入上限与幂等保护。

### 二、关键问题与调整建议
1. 问题：同步链路输入上限风险
   - 位置：`/extract/file`、`/import`
   - 风险：超大输入触发重复提交与用户误操作。
   - 建议：冻结 `maxFileSizeMb`、`maxTextChars` 与统一超限错误码；首版不设置同步超时阈值。
   - 优先级：P0

2. 问题：同步重试幂等口径
   - 位置：`/import`
   - 风险：重复确认导致重复写图。
   - 建议：强制 `Idempotency-Key`，回退键使用 `previewId+graphSpace+importMode`。
   - 优先级：P0

3. 问题：执行文档仍有历史异步词汇残留风险
   - 位置：实施任务单与拍板单
   - 风险：开发按旧口径实现任务接口，造成返工。
   - 建议：以 007/009 当前同步版为唯一基线，不再新增 `/tasks/**`。
   - 优先级：P1

### 三、最小落地路径（MVP）
- 第一步：冻结同步契约（请求上限、响应字段、错误码）。
- 第二步：后端实现同步抽取与同步导入（含幂等）。
- 第三步：前端按同步交互实现“抽取预览 -> 手动确认导入”。

### 四、待拍板决策
1. 是否确认首版不实现任务查询接口。
2. 是否确认同步链路首版不设置超时阈值。
3. 是否确认超限时仅返回错误，不做后台排队。

### 五、已落盘位置
- [docs/requirements/ARCH-REVIEW-20260505-010.md](./ARCH-REVIEW-20260505-010.md)
- [docs/requirements/index.md](./index.md)
