# DEV-FEEDBACK-20260428-014 RuntimeOptions 默认开关边界覆盖缺口

## 问题分类
- 实现风险

## 问题位置
- 需求/架构位置：`REQ-20260427-002` 第三章 P0-4（稳定性与可回归）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/main/java/com/gijela/morpheus/llm/sdk/openai/OpenAiRuntimeOptions.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/test/java/com/gijela/morpheus/llm/sdk/openai/OpenAiRuntimeOptionsTest.java`

## 影响范围
- 功能：当 metadata 非空但未显式传入 `autoToolContinue` 时，默认值行为若回归可能改变工具续跑策略。
- 稳定性：默认策略漂移会导致 chat/stream 执行语义不一致，增加灰度风险。

## 严重级别
- Minor

## 现象
- 之前已覆盖 `metadata == null` 的默认值，但缺少“metadata 非空且缺省开关键”的直接断言。

## 修正建议
- 增加定向单测，验证：
  - `autoToolContinue` 在 metadata 非空但缺省时仍为 `true`
  - 显式 `ToolContext` 且空 attributes 场景可稳定保留

## 临时绕行方案
- 无需绕行，已直接补齐单测。

## 处理结果
- 已新增测试：`shouldKeepAutoToolContinueTrueWhenMetadataPresentButKeyMissing`。
- 验证结果：
  - 执行 `mvn -pl gijela-core-llm/gijela-core-llm-sdk-openai-compatible -am test -DskipITs`
  - 结果：`Tests run: 33, Failures: 0, Errors: 0`，`BUILD SUCCESS`。
- 结论：该反馈项已闭环。
