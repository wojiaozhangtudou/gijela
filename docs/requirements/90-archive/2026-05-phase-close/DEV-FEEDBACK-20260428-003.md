# DEV-FEEDBACK-20260428-003 MockWebServer 依赖版本缺口

## 问题分类
- 实现风险

## 问题位置
- 需求/架构位置：`REQ-20260427-002` 第四章（可测试性）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/pom.xml`

## 影响范围
- 功能：无直接功能影响。
- 稳定性：阻塞测试构建，导致 Function Calling 回放测试无法执行。

## 严重级别
- Major

## 现象
- Maven 报错：`dependencies.dependency.version for com.squareup.okhttp3:mockwebserver is missing`。

## 修正建议
- 为 `mockwebserver` 显式指定版本，并与 `okhttp.version` 对齐（`${okhttp.version}`），避免不同仓库/BOM 解析差异。

## 临时绕行方案
- 无（测试链路依赖该组件）。

## 处理结果
- 已在 `openai-compatible` 模块 POM 中补充：
  - `com.squareup.okhttp3:mockwebserver:${okhttp.version}`（test scope）
- 已完成测试验证：`OpenAiCompatibleClientFunctionCallingTest`、`OpenAiRequestMapperTest`、`SseEventParserTest` 全部通过。
- 结论：该反馈项已闭环。
