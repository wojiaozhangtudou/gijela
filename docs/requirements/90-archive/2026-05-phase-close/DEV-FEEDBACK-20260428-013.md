# DEV-FEEDBACK-20260428-013 流式禁用续跑场景上下文优先级覆盖缺口

## 问题分类
- 实现风险

## 问题位置
- 需求/架构位置：`REQ-20260427-002` 第三章 P0-2（统一调用链路）与 P0-4（稳定性保障）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/test/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClientStreamFunctionCallingTest.java`

## 影响范围
- 功能：在 `stream()` 且 `autoToolContinue=false` 时，若未覆盖显式 `toolContext` 绑定行为，可能出现首轮工具上下文误用。
- 稳定性：禁用续跑属于常见治理策略，若上下文优先级回归会影响租户隔离与审计一致性。

## 严重级别
- Minor

## 现象
- 已有测试覆盖：
  - 流式显式 `toolContext` 优先
  - 流式禁用自动续跑
- 但缺少组合场景（显式 `toolContext` + `autoToolContinue=false`）的直接断言。

## 修正建议
- 增加组合场景回归测试，验证：
  - 仅发起首轮请求（不续跑）
  - 工具执行使用显式 `toolContext`

## 临时绕行方案
- 无需绕行，已直接补齐测试。

## 处理结果
- 已新增测试：`shouldUseExplicitToolContextWhenAutoContinueDisabledInStreaming`。
- 验证结果：
  - 执行 `mvn -pl gijela-core-llm/gijela-core-llm-sdk-openai-compatible -am test -DskipITs`
  - 结果：`Tests run: 32, Failures: 0, Errors: 0`，`BUILD SUCCESS`。
- 结论：该反馈项已闭环。
