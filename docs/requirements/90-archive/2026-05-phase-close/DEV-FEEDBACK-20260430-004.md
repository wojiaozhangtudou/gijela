# DEV-FEEDBACK-20260430-004

## 问题分类
- 需求缺口

## 问题位置
- 聊天会话列表展示与命名策略
- 涉及文件：
  - gijela-core/gijela-core-chat/src/main/java/com/gijela/morpheus/chat/service/impl/RedisConversationStore.java

## 影响范围
- 功能/体验：会话默认标题均为“新会话”，用户难以快速识别历史会话内容

## 严重级别
- Major

## 修正建议
1. 会话首次写入用户消息时，自动以首条用户消息生成会话标题。
2. 仅在标题为空或默认值“新会话”时重命名，避免覆盖用户后续可能的人工命名。
3. 标题长度裁剪，避免超长文案影响列表展示。

## 临时绕行方案
- 无需绕行，已直接修复：默认标题会在首条用户消息后自动替换。

## 本次落地结果
- 已实现自动命名逻辑，规则：取首条 `user` 消息，去换行并截断至 24 字。
- 已补充单元测试：`appendMessages_shouldRenameDefaultTitleWithFirstUserMessage`。
