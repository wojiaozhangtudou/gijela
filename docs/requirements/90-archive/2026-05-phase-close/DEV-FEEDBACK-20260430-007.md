# DEV-FEEDBACK-20260430-007

## 问题分类
- 架构缺口

## 问题位置
- Chat 编排层检索触发策略
- 涉及文件：
  - gijela-core/gijela-core-chat/src/main/java/com/gijela/morpheus/chat/service/impl/ChatOrchestratorServiceImpl.java

## 影响范围
- 功能：编排层预检索会覆盖“由模型决定是否调用知识库工具”的策略。
- 性能：非必要查询也会触发 embedding + 向量检索。

## 严重级别
- Major

## 修正建议
1. 流式场景优先采用“模型工具调用驱动检索”：仅在 `tool_result` 中出现 `hits` 时回填 references。
2. 编排层保留轻量兜底（如寒暄词过滤）以控制成本与噪音。

## 临时绕行方案
- 无需绕行，已落地：stream 模式改为根据 `tool_result.result.hits` 生成 references。

## 本次落地结果
- stream 不再预先调用 `knowledgeIngestionService.search()`。
- 新增测试覆盖 `tool_result -> done.references` 回传与持久化。
