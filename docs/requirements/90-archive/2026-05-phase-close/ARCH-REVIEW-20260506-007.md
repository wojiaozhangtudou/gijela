# ARCH-REVIEW-20260506-007

## 一、可落地性结论
- 结论：**可落地**。
- 依据：前置评审（004/005/006）已冻结范围与契约，当前可进入 Day1 实施；建议按“后端先通、前端后接、最后联调”推进。

## 二、关键问题与调整建议

1. 问题：任务粒度仍偏大，容易多人并行冲突。
   - 位置：第2章 2.1（架构边界与模块职责）
   - 风险：多人同时改 `ChatController` / `ChatOrchestratorServiceImpl` 引发冲突。
   - 建议：Day1 严格拆分为 3 条并行泳道：
     - A：数据模型与持久化
     - B：接口层与权限校验
     - C：前端 API/Store 壳子
   - 优先级：P0

2. 问题：方法签名未冻结，开发中可能反复改 DTO。
   - 位置：第3章 3.1（协议与数据契约）
   - 风险：联调时前后端字段名不一致。
   - 建议：先冻结 Service 方法签名（见第六章）。
   - 优先级：P0

3. 问题：上线门槛未体现到 Day1 验收标准。
   - 位置：第5章 5.1（可测试性）
   - 风险：提交代码后仍不可联调。
   - 建议：Day1 必过 4 条验收（见第七章），不过不合并。
   - 优先级：P1

## 三、最小落地路径（MVP）
- 第一步：完成后端 Entity/Mapper/DDL 与基础 Service（可插入/查询/更新版本）。
- 第二步：完成 `ChatPromptController` 四个接口与错误码映射。
- 第三步：前端接 `src/api/prompt.ts` + `src/store/prompt.ts`，页面先显示后编辑。

## 四、待拍板决策
1. Day1 是否包含“发布回滚”接口（建议：不含，放 Day2）。
2. 会话级提示词长度上限（建议：2000 字符）。
3. 系统级草稿是否允许空值保存（建议：允许草稿空，发布时禁止空）。

## 五、Day1 开工清单（按人天）

### 5.1 后端 A（1 人日）
- 新增实体：
  - `domain/entity/ChatPromptSystem.java`
  - `domain/entity/ChatPromptSystemAudit.java`
  - `domain/entity/ChatSessionPrompt.java`
- 新增 Mapper：
  - `mapper/ChatPromptSystemMapper.java`
  - `mapper/ChatPromptSystemAuditMapper.java`
  - `mapper/ChatSessionPromptMapper.java`
- DDL（同仓脚本目录按现有规范落地）：
  - `chat_prompt_system`
  - `chat_prompt_system_audit`
  - `chat_session_prompt`

### 5.2 后端 B（1 人日）
- 新增 DTO：
  - `domain/dto/prompt/SystemPromptDraftUpdateRequest.java`
  - `domain/dto/prompt/SystemPromptPublishRequest.java`
  - `domain/dto/prompt/SessionPromptUpdateRequest.java`
- 新增 VO：
  - `domain/vo/prompt/SystemPromptCurrentResponse.java`
  - `domain/vo/prompt/SessionPromptResponse.java`
- 新增 Service：
  - `service/PromptConfigService.java`
  - `service/impl/PromptConfigServiceImpl.java`
- 新增 Controller：
  - `controller/ChatPromptController.java`

### 5.3 前端 C（0.5~1 人日）
- 新增 API：
  - `gijela-bloom/gijela-bloom-chat/src/api/prompt.ts`
- 新增 Store：
  - `gijela-bloom/gijela-bloom-chat/src/store/prompt.ts`
- 修改页面：
  - `gijela-bloom/gijela-bloom-chat/src/views/ChatPage.vue`
  - 增加“系统提示词”按钮（抽屉）
  - 增加“会话提示词”按钮（侧栏）

## 六、方法签名冻结（Day1）

### 6.1 `PromptConfigService`
- `SystemPromptCurrentResponse getSystemCurrent(String tenantId, String appCode, String modelRoute);`
- `SystemPromptCurrentResponse saveSystemDraft(String tenantId, String operator, SystemPromptDraftUpdateRequest request);`
- `SystemPromptCurrentResponse publishSystemPrompt(String tenantId, String operator, SystemPromptPublishRequest request);`
- `SessionPromptResponse updateSessionPrompt(String tenantId, String sessionId, String operator, SessionPromptUpdateRequest request);`
- `String resolveEffectivePrompt(String tenantId, String sessionId, String appCode, String modelRoute);`
- `String resolveEffectivePromptHash(String tenantId, String sessionId, String appCode, String modelRoute);`

### 6.2 `ChatPromptController`
- `GET /api/v1/chat/prompts/system/current`
- `PUT /api/v1/chat/prompts/system/draft`
- `POST /api/v1/chat/prompts/system/publish`
- `PUT /api/v1/chat/sessions/{sessionId}/prompt`

## 七、Day1 验收标准（不过不合并）
1. `mvn -pl gijela-core-chat -am test` 通过。
2. 四个提示词接口可调通，409 冲突可复现。
3. 前端可读取并保存系统草稿、会话提示词。
4. 对话发送时日志可见 `effectivePromptHash`。

## 八、已落盘位置
- docs/requirements/ARCH-REVIEW-20260506-007.md
- docs/requirements/index.md
