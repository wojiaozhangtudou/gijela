# DEV-FEEDBACK-20260428-011 显式 ToolContext 优先级回归覆盖缺口

## 问题分类
- 实现风险

## 问题位置
- 需求/架构位置：`REQ-20260427-002` 第三章 P0-2（统一调用链路）与 P0-4（稳定性保障）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/main/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClient.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/test/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClientFunctionCallingTest.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/README.md`

## 影响范围
- 功能：当 `toolContext` 与 `tenantId` 同时存在时，若优先级行为回归，工具执行上下文可能错误。
- 稳定性：上下文污染会影响多租户隔离与工具调用可追溯性。

## 严重级别
- Minor

## 现象
- 原有测试覆盖“默认回退上下文”，但缺少“显式 `toolContext` 优先于 metadata 回退”的直接断言。

## 修正建议
- 增加回归用例，验证显式 `toolContext` 的 `tenantId/attributes` 被优先使用。
- README 增补显式 `toolContext` 示例，减少调用方误解。

## 临时绕行方案
- 无需绕行，已直接补齐测试和文档。

## 处理结果
- 已新增测试：`shouldPreferExplicitToolContextOverMetadataFallback`。
- 已更新 README：补充显式 `toolContext` 示例。
- 验证结果：
  - 执行 `mvn -pl gijela-core-llm/gijela-core-llm-sdk-openai-compatible -am test -DskipITs`
  - 结果：`Tests run: 30, Failures: 0, Errors: 0`，`BUILD SUCCESS`。
- 结论：该反馈项已闭环。
