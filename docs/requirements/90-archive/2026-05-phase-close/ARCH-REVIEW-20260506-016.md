# ARCH-REVIEW-20260506-016

## 一、可落地性结论
- 结论：**可落地**。
- 依据：当前已完成从方案到发布复盘的治理闭环；本文件补齐“开发开工包”，让团队从文档直接切到编码。

## 二、关键问题与调整建议

1. 问题：文档齐全但开发起手仍需自行拼装模板。
   - 位置：第2章 2.1（可测试性与上线门槛）
   - 风险：不同人按不同习惯开工，代码风格和契约漂移。
   - 建议：提供统一开工包：目录模板、类名模板、提交规范模板。
   - 优先级：P0

2. 问题：分支与提交策略未固化到本需求。
   - 位置：第3章 3.1（架构边界与模块职责）
   - 风险：大提交难回滚，评审效率低。
   - 建议：按 Story 拆提交：每个 Story 至少 1 个可独立回滚提交。
   - 优先级：P1

## 三、最小落地路径（MVP）
- 第一步：按第五章创建代码骨架文件（空实现也可）。
- 第二步：按第六章“提交批次建议”逐批实现并合并。
- 第三步：按第七章“联调最小脚本”完成端到端冒烟。

## 四、待拍板决策
1. 分支模型：单分支串行还是 Story 分支并行（建议：Story 并行）。
2. 提交规范是否强制 Conventional Commits（建议：强制）。
3. 是否启用“未过 Gate 禁止合并”钩子（建议：启用）。

## 五、开工包（代码骨架清单）

### 5.1 后端骨架（gijela-core-chat）
- 新增：`domain/entity/ChatPromptSystem.java`
- 新增：`domain/entity/ChatPromptSystemAudit.java`
- 新增：`domain/entity/ChatSessionPrompt.java`
- 新增：`mapper/ChatPromptSystemMapper.java`
- 新增：`mapper/ChatPromptSystemAuditMapper.java`
- 新增：`mapper/ChatSessionPromptMapper.java`
- 新增：`domain/dto/prompt/SystemPromptDraftUpdateRequest.java`
- 新增：`domain/dto/prompt/SystemPromptPublishRequest.java`
- 新增：`domain/dto/prompt/SystemPromptRollbackRequest.java`
- 新增：`domain/dto/prompt/SessionPromptUpdateRequest.java`
- 新增：`domain/vo/prompt/SystemPromptCurrentResponse.java`
- 新增：`domain/vo/prompt/SessionPromptResponse.java`
- 新增：`service/PromptConfigService.java`
- 新增：`service/impl/PromptConfigServiceImpl.java`
- 新增：`controller/ChatPromptController.java`

### 5.2 前端骨架（gijela-bloom-chat）
- 新增：`src/api/prompt.ts`
- 新增：`src/store/prompt.ts`
- 修改：`src/views/ChatPage.vue`（添加系统级/会话级入口）

### 5.3 配置与脚本
- 修改：`application-dev.yml`（灰度开关）
- 修改：`application.yml`（默认阈值）
- 新增：数据库迁移脚本（按仓库既有规范路径）

## 六、提交批次建议（按可回滚单元）
1. `feat(chat-prompt): add prompt entities and mappers`
2. `feat(chat-prompt): add prompt service and controller apis`
3. `feat(chat-prompt): add conflict payload and rollback endpoint`
4. `feat(chat-prompt): wire effectivePromptHash in orchestrator`
5. `feat(chat-prompt-ui): add prompt api/store and chat page panels`
6. `chore(chat-prompt): add gray config and release checklist updates`

## 七、联调最小脚本（冒烟）
1. 创建系统草稿 -> 发布。
2. 创建会话级提示词 -> 发送一条消息验证生效。
3. 构造版本冲突 -> 验证 409 与最新版本载荷。
4. 执行回滚 -> 验证版本回退。
5. 开灰度 -> 验证命中租户与非命中租户差异。

## 八、验收完成定义（DoD）
1. 后端测试通过，前端构建通过。
2. 关键接口联调通过（current/draft/publish/session/rollback）。
3. 审计可追溯（含 diff 与 `effectivePromptHash`）。
4. 回滚演练通过（≤10 分钟）。

## 九、已落盘位置
- docs/requirements/ARCH-REVIEW-20260506-016.md
- docs/requirements/index.md
