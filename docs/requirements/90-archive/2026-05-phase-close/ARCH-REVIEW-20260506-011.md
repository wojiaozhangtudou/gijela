# ARCH-REVIEW-20260506-011

## 一、可落地性结论
- 结论：**可落地**。
- 依据：前序文档（004~010）已冻结范围、契约、排期与闸门；当前可直接转 Jira 执行，避免口头任务失真。

## 二、关键问题与调整建议

1. 问题：研发任务仍以文档段落组织，缺少工单结构。
   - 位置：第2章 2.1（目标与范围一致性）
   - 风险：执行时边界不清，验收标准不一致。
   - 建议：按 Jira 三层拆分：`Epic -> Story -> Sub-task`，并给每条 Story 明确 DoD。
   - 优先级：P0

2. 问题：后端/前端/测试依赖关系未显式建图。
   - 位置：第2章 2.2（架构边界与模块职责）
   - 风险：并行开发被隐式依赖阻塞。
   - 建议：在 Jira 中标注阻塞关系：
     - FE Story 阻塞于 BE 接口 Story
     - 发布 Story 阻塞于回滚与观测 Story
   - 优先级：P0

3. 问题：风险闸门未映射到工单完成条件。
   - 位置：第5章 5.1（稳定性）
   - 风险：任务关闭但系统不可发布。
   - 建议：把 P0 Gate 写入 Story DoD：
     - 单测/集成通过
     - 409 冲突可复现
     - 回滚演练通过
   - 优先级：P0

## 三、最小落地路径（MVP）
- 第一步：创建 1 个 Epic + 6 个 Story。
- 第二步：按 Day1~Day3 将 Story 拆分为 Sub-task 并分配负责人。
- 第三步：每天按 Gate 执行“是否可进入下一天”评审。

## 四、待拍板决策
1. 工单估点采用人天还是 Story Point（建议：本轮用人天）。
2. 是否启用“阻塞项未清空禁止开始新 Story”（建议：启用）。
3. 是否将回滚演练设为发布前强制检查项（建议：强制）。

## 五、Jira 任务拆解（可直接抄录）

### Epic
- **EPIC-CHAT-PROMPT-001**：提示词双层配置能力上线（系统级 + 会话级）

### Story 1（BE）
- **STORY-001**：提示词数据模型与持久化
- 范围：Entity/Mapper/DDL（system/session/audit）
- DoD：
  - 建表成功并可读写
  - `mvn -pl gijela-core-chat -am test` 通过

Sub-task：
1. 新增 `ChatPromptSystem` / `ChatSessionPrompt` / `ChatPromptSystemAudit`
2. 新增对应 Mapper
3. 提交 DDL 与本地迁移验证

### Story 2（BE）
- **STORY-002**：系统级与会话级提示词 API
- 范围：4 个基础接口（current/draft/publish/session）
- DoD：
  - 接口联调通过
  - 统一 `ApiResponse` 与错误码

Sub-task：
1. 新增 `ChatPromptController`
2. 新增 DTO/VO（prompt 领域）
3. 接入权限点 `prompt.system.read/write`、`prompt.session.write`

### Story 3（BE）
- **STORY-003**：冲突处理 + 审计 diff + 回滚
- 范围：409 冲突载荷、审计差异、rollback API
- DoD：
  - 冲突可复现且返回最新版本载荷
  - 回滚可将版本 N 恢复到 N-1

Sub-task：
1. `PROMPT_VERSION_CONFLICT` 载荷增强
2. `diffSummary` 生成与审计查询
3. 新增 `POST /prompts/system/rollback`

### Story 4（BE）
- **STORY-004**：灰度开关与编排层观测标签
- 范围：gray 策略与 `effectivePromptHash` 链路
- DoD：
  - 灰度租户命中新版本，非灰度租户不受影响
  - 审计可追踪 `promptPublishedVersion/grayHit/effectivePromptHash`

Sub-task：
1. `ChatModuleProperties` 新增 gray 配置
2. `ChatOrchestratorServiceImpl` 接入灰度决策
3. 审计字段扩展

### Story 5（FE）
- **STORY-005**：对话页提示词编辑交互
- 范围：系统抽屉 + 会话侧栏
- DoD：
  - 可读取/保存系统草稿
  - 可编辑会话提示词并影响后续消息

Sub-task：
1. 新增 `src/api/prompt.ts`
2. 新增 `src/store/prompt.ts`
3. 修改 `src/views/ChatPage.vue` 增加入口与表单

### Story 6（BE+FE+QA）
- **STORY-006**：审批发布流程 + 告警 + 回滚演练
- 范围：submit/approve/publish-approved、告警阈值、演练记录
- DoD：
  - 提交人与审批人不可同人
  - 观测支持按版本过滤
  - 演练全流程 ≤ 10 分钟

Sub-task：
1. 审批状态机与接口
2. 前端审批按钮与状态展示
3. 监控面板与告警规则
4. 回滚演练记录模板与一次实测

## 六、依赖关系（Jira 链接建议）
1. STORY-005 依赖 STORY-002。
2. STORY-006 依赖 STORY-003 + STORY-004 + STORY-005。
3. STORY-004 依赖 STORY-001（数据可读取）。

## 七、上线门槛（工单关闭前必查）
1. 所有 Story 满足 DoD。
2. P0 Gate 全绿：测试通过、冲突可复现、回滚可用。
3. 生产灰度发布比例与回滚预案已审批。

## 八、已落盘位置
- docs/requirements/ARCH-REVIEW-20260506-011.md
- docs/requirements/index.md
