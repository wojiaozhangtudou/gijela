# DEV-FEEDBACK-20260428-009 ToolContext 回退链路测试覆盖缺口

## 问题分类
- 实现风险

## 问题位置
- 需求/架构位置：`REQ-20260427-002` 第三章 P0-4（稳定性与可观测）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/main/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClient.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-openai-compatible/src/test/java/com/gijela/morpheus/llm/sdk/openai/OpenAiCompatibleClientFunctionCallingTest.java`

## 影响范围
- 功能：当 metadata 类型化重构后，若 `toolContext` 未显式传入，默认上下文回退行为缺少自动化保护。
- 稳定性：后续重构可能误删 `tenantId/attributes` 透传逻辑，引发工具执行上下文丢失。

## 严重级别
- Minor

## 现象
- 原有测试覆盖了 Function Calling 成功/禁用自动续跑，但未覆盖“仅提供 metadata、不提供显式 `toolContext`”时的默认回退上下文构建。

## 修正建议
- 补充回归用例：验证 `tenantId` 与 metadata attributes 在默认 `ToolContext` 中可见。
- 在后续重构中将该测试纳入必跑基线，避免上下文透传回归。

## 临时绕行方案
- 无需绕行，已直接补齐自动化测试。

## 处理结果
- 已新增测试：`shouldBuildDefaultToolContextFromMetadataWhenNoExplicitContext`。
- 已同步清理 `OpenAiCompatibleClient` 中 runtime options 重构后的未使用 metadata 常量与冗余参数。
- 验证结果：
  - 执行 `mvn -pl gijela-core-llm/gijela-core-llm-sdk-openai-compatible -am test -DskipITs`
  - 结果：`Tests run: 26, Failures: 0, Errors: 0`，`BUILD SUCCESS`。
- 结论：该反馈项已闭环。
