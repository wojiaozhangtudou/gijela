# DEV-FEEDBACK-20260428-022 chat 前端 TypeScript 语言服务类型解析异常

## 一、问题分类
- 实现风险

## 二、问题位置
- 涉及文件：
  - `gijela-bloom/gijela-bloom-chat/tsconfig.json`
  - `gijela-bloom/gijela-bloom-chat/src/types/vendor-modules.d.ts`
- 现象位置：
  - `src/store/chat.ts`
  - `src/views/ChatPage.vue`

## 三、影响范围
- 功能：不阻塞运行与打包。
- 可维护性：IDE/语言服务报错会影响开发体验和代码提示质量。

## 四、严重级别
- Minor

## 四点一、处理优先级
- P1（不阻塞当前联调，但需在上线前消风险）

## 五、问题说明
- 在 `pnpm build` 与 `pnpm exec tsc --noEmit` 可通过的情况下，编辑器问题面板持续报 `vue/pinia/element-plus` 类型声明缺失。
- 当前先通过 `tsconfig` 调整和本地声明文件兜底，消除误报并保证可持续开发。

## 六、修正建议
1. 后续统一前端 TypeScript/VSCode 版本与工程模板，避免语言服务与构建器的解析差异。
2. 条件允许时移除 `vendor-modules.d.ts` 兜底声明，恢复官方类型提示。
3. 在仓库层补充前端环境校验脚本，固定 `pnpm` 与 TypeScript 解析配置。

## 七、临时绕行方案
- 保留 `vendor-modules.d.ts` 与当前 `tsconfig` 设置，优先保证联调与交付节奏。

## 八、闭环计划（可执行）
1. 基线对齐：统一 VS Code TypeScript SDK 与工作区 TypeScript 版本，固化为仓库推荐版本。
2. 工程收敛：补充前端环境校验脚本（检查 `pnpm`、`typescript`、`moduleResolution`）。
3. 回收兜底：分步骤移除 `vendor-modules.d.ts`，每移除一类声明即执行 `pnpm exec tsc --noEmit` 与 `pnpm build`。
4. 门禁固化：将 `pnpm exec tsc --noEmit` 纳入 CI 必过项，避免仅靠编辑器状态判断。

## 九、退出标准（Done）
- 在不依赖 `vendor-modules.d.ts` 的情况下：
  - `pnpm exec tsc --noEmit` 通过；
  - `pnpm build` 通过；
  - 编辑器问题面板不再出现 `vue/pinia/element-plus` 类型缺失误报。

## 十、当前进展（2026-04-28）
- 已完成：
  1. 工作区 TypeScript SDK 固定为项目本地版本（`.vscode/settings.json`）。
  2. 前端新增 `typecheck` 脚本并本地验证通过。
  3. 新增 CI 工作流 `chat-ci.yml`，将 `pnpm typecheck`、`pnpm build`、`mvn -pl gijela-core-chat -am compile` 纳入门禁。
  4. `vendor-modules.d.ts` 已下线：从 `tsconfig` 编译范围排除，并清空为归档注释；`typecheck/build` 持续通过。
- 未完成：
  1. 无（当前按既定退出标准已闭环）。

## 十点一、闭环结论
- 结论：已闭环。
- 依据：
  - `pnpm typecheck` 通过；
  - `pnpm build` 通过；
  - 问题面板当前无相关错误。

## 十一、已落盘位置
- `docs/requirements/DEV-FEEDBACK-20260428-022.md`
