# DEV-FEEDBACK-20260429-002 全仓长期需求与当前实现存在闭环缺口

## 一、问题分类
- 实现风险

## 二、问题位置
- 需求章节：`REQ-20260427-002`（P0 功能清单第 8~14 条）
- 涉及文件：
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-plugin/src/main/java/com/gijela/morpheus/llm/sdk/plugin/PluginManager.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-plugin/src/main/java/com/gijela/morpheus/llm/sdk/plugin/PluginRegistry.java`
  - `gijela-core/gijela-core-llm/gijela-core-llm-sdk-plugin/src/main/java/com/gijela/morpheus/llm/sdk/plugin/PluginManifest.java`

## 三、影响范围
- 功能：插件热插拔、隔离治理、预算配额、版本兼容等长期能力尚未形成完整可验收闭环。
- 稳定性：缺少插件级健康检查/故障隔离/自动回滚策略实现，线上扩展能力存在放大风险。
- 可测试性：`gijela-core-llm-sdk-plugin` 与 `gijela-core-llm-sdk-observability` 目前无测试用例，回归护栏不足。

## 四、严重级别
## 四、严重级别
- Blocker

## 八、处理结果
- 本轮已完成状态核验：
  - `gijela-core-chat` 扩展链路已补齐：对象存储改为真实 HTTP 上传调用、知识检索改为 Qdrant HTTP 查询、Flyway 迁移已接入；
  - `gijela-core-chat` 全量测试通过（24/24）；
  - 前端 `pnpm typecheck`、`pnpm build` 均通过。
- 仍需注意：`REQ-20260427-002` 的全仓长期能力不只 chat 模块，LLM plugin/observability 长期需求仍需继续推进。
- 当前状态：chat 范围已关闭；全仓长期需求仍进行中。
  2. 热插拔“新请求新版本、在途请求旧版本、失败自动回滚”运行约束；
  3. 插件清单（Manifest）兼容校验与信任校验（白名单 + SHA-256）链路；
  4. 租户预算上限、超额限流/降级/告警的执行闭环。

## 六、修正建议
1. 先按 P0 最小可验收补齐插件治理主链路：
   - `PluginPolicyService`（权限 + 信任校验 + 版本兼容）；
   - `PluginRuntimeSwitcher`（新旧版本切换与回滚）；
   - `PluginHealthMonitor`（健康检查与熔断隔离）。
2. 补齐预算治理执行器：
   - 以租户维度接入预算阈值检查，触发限流/降级/告警。
3. 为 plugin/observability 模块补最小关键测试集：
   - 生命周期状态机；
   - 热切换在途请求保持；
   - 预算超限策略断言；
   - 权限拒绝与审计记录断言。

## 七、临时绕行方案
- 当前可继续沿“chat 主链路冻结交付”策略运行；
- 但不得宣称“按全仓长期需求全部开发完成”，应标记为“长期能力进行中”。

## 八、处理结果
- 本轮已完成状态核验：
  - `gijela-core-chat` 关键测试集通过；
  - `gijela-core-llm` 聚合测试通过（其中 openai-compatible 测试通过，plugin/observability 当前无测试）。
- 当前状态：已记录。

## 九、已落盘位置
- `docs/requirements/DEV-FEEDBACK-20260429-002.md`
