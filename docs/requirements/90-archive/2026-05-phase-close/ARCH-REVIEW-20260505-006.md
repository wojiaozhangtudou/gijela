# ARCH-REVIEW-20260505-006：Neo4j 图谱能力拍板单（可直接确认）

- 日期：2026-05-05
- 状态：已确认
- 关联：
  - [ARCH-REVIEW-20260505-004.md](./ARCH-REVIEW-20260505-004.md)
  - [ARCH-REVIEW-20260505-005.md](./ARCH-REVIEW-20260505-005.md)

### 一、可落地性结论
- 结论：**可落地（待你完成拍板）**。
- 依据：
  1. 方案、风险、Gate、参数均已收敛；
  2. 当前仅差“最终决策确认”，确认后即可进入编码阶段。

### 二、拍板项（请逐条确认）

> 建议填写：`确认/否决 + 备注`

1. 模式冲突规则
- 结论建议：三种 `extractMode` 均“抽取仅预览，入图必须调用 `/import` 确认”
- 你的决定：确认
- 备注：按业界常见审核链路，抽取与导入解耦，禁止旁路入图。

2. 文件抽取执行模型
- 结论建议：`/extract/file` 全量同步返回预览（超限直接报错）
- 你的决定：确认
- 备注：首版采用同步预览；不新增后台任务；不额外设置应用层同步超时阈值。

3. 导入幂等策略
- 结论建议：优先 `Idempotency-Key`，缺省回退 `previewId+graphSpace+importMode`，窗口 24h
- 你的决定：确认
- 备注：按业界常见写入防重策略，`/import` 强制要求 `Idempotency-Key`。

4. 关系业务主键
- 结论建议：`relationshipId=SHA256(tenantId|graphSpace|sourceNormalized|targetNormalized|descriptionNormalized)`
- 你的决定：确认
- 备注：采用稳定业务键，避免依赖 Neo4j 内部 `id()`。

5. 首版容量阈值
- 结论建议：文件 <=10MB；文本 <=50k；实体 <=300；关系 <=800；`maxTokens<=4096`
- 你的决定：确认
- 备注：按首版可控成本与同步交互体验冻结。

6. Prompt 调试开关
- 结论建议：`promptOverride` 仅灰度账号 + 权限 `graph:debug-prompt` + 审计必记
- 你的决定：确认
- 备注：按业界常见高风险调试开关治理方式执行。

### 三、最小落地路径（拍板后）
- 第一步：冻结本拍板单（状态改为“已确认”）。
- 第二步：后端先落 G1~G4（模式冲突、同步入参保护、幂等、schema init）。
- 第三步：前端落 GraphPage 基础功能（文本/文件抽取、预览、确认入图）。
- 第四步：联调验收通过后，灰度开放 `promptOverride`。

### 四、待拍板决策（汇总）
1. 已确认：所有模式都必须人工确认后入图。
2. 已确认：文件抽取同步化，首版不建设后台任务与任务查询接口。
3. 已确认：幂等窗口 24h，`/import` 强制 `Idempotency-Key`。
4. 已确认：`relationshipId` 按哈希规则生成。
5. 已确认：首版容量阈值采用 10MB / 50k / 300 / 800 / 4096。
6. 已确认：`promptOverride` 仅灰度账号 + 权限控制 + 审计。

### 五、已落盘位置
- [docs/requirements/ARCH-REVIEW-20260505-006.md](./ARCH-REVIEW-20260505-006.md)
- [docs/requirements/index.md](./index.md)
